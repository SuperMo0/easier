Dear Cohere team,

North puts agents to work inside enterprise systems, where the bar is reliable, observable and auditable from day one. I build LLM features under that bar now. At Sync NGO I built the AI features of a recruitment platform used in production by enterprise clients: semantic search on pgvector with HNSW indexing and correct filtered retrieval, an LLM pipeline that turns unstructured CVs into typed schemas, and a scoring pipeline that rates candidates against role criteria and shows the evidence for each score. I own those features end to end, from the Python API and worker to the React portals, behind 117 automated tests.

On my own I built a LangGraph research agent that routes each query, runs parallel web searches and grades its own answer before returning it, with Pydantic models and Tenacity retries.

Forward-deployed work, framing an ambiguous customer problem and taking it to production, is the job I want. I live in Dubai and can start about 20 days after an offer.

Mwafak Almahaini

Dear talabat team,

talabat gets food and groceries to customers across the region in minutes, and I am building a smaller version of the same problem: a last-mile delivery platform where merchants create shipments, trips are planned by zone, vehicle capacity and driver hours, and every parcel moves through an explicit state machine from pickup to delivery. It is event-driven on Kafka, and shipment creation is idempotent, so a retried request never creates a second parcel.

At Sync NGO I build the backend of a recruitment platform used in production by enterprise clients: a REST API in ASP.NET Core over PostgreSQL, per-tenant rate limiting, Redis caching, and xUnit suites gating every deploy through GitHub Actions. I have also written a Go routing service that assigns orders to warehouses, using Kafka consumer groups and an outbox pattern so no order is lost.

A Software Engineer I role with .NET and Go, on a team that ships in small batches, is where I want to grow. I live in Dubai and can start about 20 days after an offer.

Mwafak Almahaini
